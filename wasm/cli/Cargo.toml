[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { workspace = true, features = ["derive", "std", "string"] }
seda-runtime-sdk = { workspace = true, features = ["wasm"] }
